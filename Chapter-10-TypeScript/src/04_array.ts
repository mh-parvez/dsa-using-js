const numbers: number[] = [4, 5, 5, 6, 6, 6];
//const numbers:<number> = [4, 5, 5, 6, 6, 6];
console.log(numbers);

const modifedNumber: number[] = numbers.map((num: number) => num * num);
console.log(modifedNumber);

